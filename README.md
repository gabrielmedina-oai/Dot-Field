
  # Redesign web tool UI

  This is a code bundle for Redesign web tool UI. The original project is available at https://www.figma.com/design/dSs1WM3FJor7roiLQJJilm/Redesign-web-tool-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  